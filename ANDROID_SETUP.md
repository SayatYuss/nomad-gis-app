# Android Setup Guide

## Требуемые разрешения

Приложение Nomad GIS использует следующие разрешения на Android:

### 📍 GPS и Локация
- **ACCESS_FINE_LOCATION** - точное определение местоположения (GPS)
- **ACCESS_COARSE_LOCATION** - приблизительное определение местоположения (сеть/мобильная сеть)
- **ACCESS_BACKGROUND_LOCATION** - доступ к местоположению в фоне

### 🌐 Интернет
- **INTERNET** - доступ в интернет для API запросов

### 💾 Хранилище
- **READ_EXTERNAL_STORAGE** - чтение файлов (для загрузки изображений аватара)
- **WRITE_EXTERNAL_STORAGE** - запись файлов (для кеширования)

### 📷 Камера
- **CAMERA** - доступ к камере (для выбора/загрузки фото профиля)

### 📡 Сеть
- **ACCESS_NETWORK_STATE** - проверка статуса сети
- **CHANGE_NETWORK_STATE** - изменение параметров сети

## Автоматическое запрашивание разрешений

Приложение использует пакет `geolocator` для управления разрешениями GPS. При первом запуске в основных функциях:

1. **Инициализация локации** - приложение запросит разрешение на доступ к GPS
2. **Загрузка аватара** - при выборе фото будет запрошено разрешение на доступ к хранилищу и камере
3. **Проверка локации** - при нажатии на кнопку проверки будет проверено разрешение GPS

## Версия SDK

- **Минимальная версия (minSdkVersion)**: определяется Flutter
- **Целевая версия (targetSdkVersion)**: определяется Flutter
- **Компиляемая версия (compileSdkVersion)**: определяется Flutter

## Файл AndroidManifest.xml

Все разрешения объявлены в `android/app/src/main/AndroidManifest.xml`:

```xml
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
```

## Обработка разрешений в коде

### LocationService
Файл `lib/services/location_service.dart` содержит методы для:
- `requestLocationPermission()` - запрос разрешения на GPS
- `isLocationServiceEnabled()` - проверка включено ли GPS
- `getCurrentPosition()` - получение текущего местоположения
- `getPositionStream()` - получение потока изменений местоположения

### Примеры использования

```dart
// Запрос разрешения на GPS
final hasPermission = await LocationService.requestLocationPermission();

// Получение текущей позиции
final position = await LocationService.getCurrentPosition();

// Расчет расстояния
final distance = LocationService.calculateDistance(
  lat1, lon1,
  lat2, lon2
);
```

## Тестирование на устройстве

### Проверка разрешений в Android Studio
1. Откройте логи: `adb logcat`
2. Запустите приложение на устройстве
3. При первом запуске должен появиться диалог запроса разрешений
4. Разрешите все требуемые разрешения

### Отключение GPS для тестирования
На эмуляторе можно отключить GPS:
```bash
adb shell settings put secure location_providers_allowed -gps
```

### Включение GPS для тестирования
```bash
adb shell settings put secure location_providers_allowed +gps
```

## Советы по оптимизации

1. **GPS в фоне** - приложение проверяет GPS каждые 10 секунд (настраивается в `utils/constants.dart`)
2. **Точность** - используется `LocationAccuracy.high` для точных координат
3. **Фильтр расстояния** - игнорируются изменения менее 10 метров

## Проблемы и решения

### GPS не работает на эмуляторе
- Убедитесь, что эмулятор поддерживает GPS (Google Play System Image)
- Включите GPS в настройках эмулятора
- Используйте `adb emu geo fix <latitude> <longitude>` для имитации GPS

### Разрешения не запрашиваются
- Проверьте Android версию устройства (6.0+ требует runtime permissions)
- Очистите данные приложения и переустановите его
- Проверьте логи приложения в Android Studio

### Высокое потребление батареи
- Проверьте интервал проверки GPS в `utils/constants.dart`
- Убедитесь, что GPS отключается при выходе из приложения
- Используйте более низкую точность (`LocationAccuracy.medium`) для фонового режима

## Ссылки

- [Android Permissions Guide](https://developer.android.com/guide/topics/permissions/overview)
- [Geolocator Plugin](https://pub.dev/packages/geolocator)
- [Flutter Location Services](https://flutter.dev/docs/development/data-and-backend/firebase)
